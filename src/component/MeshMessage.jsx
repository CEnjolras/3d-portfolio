import { Html } from '@react-three/drei'
import Message from './Message'

export default function MeshMessage({children, className,...props}) {
  return (
    <>
      {/*
        <Html {...props}>
          <Message className={className}>
            {children}
          </Message>
        </Html>
      */}
    </>
  )
}
